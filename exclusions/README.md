# Exclusions
A recommended tap exclusions file (`tap_exclusions.json`) is provided here. To use the file, place a copy of it in your workspace directory (by default `~/.pywerfl`, which on Windows by default resolves to `C:\Users\<username>\.pywerfl`).
